---
interface Props {
  date: string;
  title: string;
  description: string;
  link: string;
}
const { date, title, description, link } = Astro.props;
---

<div class="flex items-center">
  <div
    class="z-10 flex items-center justify-center w-6 h-6 rounded-full ring-0 ring-white bg-orange-600 sm:ring-8 dark:ring-gray-900 shrink-0"
  >
  </div>
  <div class="hidden sm:flex w-full bg-gray-200 h-0.5 dark:bg-gray-700"></div>
</div>
<div class="mt-3 sm:pe-8">
  <h3 class="text-5xl font-semibold text-gray-900 dark:text-white">{title}</h3>
  <time
    class="block mb-2 text-3xl font-semibold leading-none text-gray-400 dark:text-gray-500"
    >{date}</time
  >
  <p class="text-4xl font-normal text-gray-500 dark:text-gray-400">
    {description}
  </p>
</div>
